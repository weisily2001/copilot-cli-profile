# Copilot CLI 多 Agent 动态协同编排设计

## 1. 目标

在昨天已完成的全局行为内核、记忆层、技能层和模板验证基础上，为 Copilot CLI 增加一套可控的多 agent 协同并发机制，重点解决：

1. 大任务执行速度不够快
2. 单窗口上下文容易过载
3. 并发执行缺乏统一调度与成本约束

本轮目标不是追求最大并发数，而是让系统在**完成任务的前提下，以更低的上下文负担和可控 token 成本提升整体效率**。

## 2. 范围

本轮只覆盖多 agent 动态协同框架 v1：

- 主控 agent 任务画像
- 动态选择并发模式
- token 预算门控
- 子 agent 统一任务契约
- 结果汇总与冲突收敛
- 基础观测指标

本轮不覆盖：

- 无约束 swarm 式自由协同
- 多个 agent 同时修改同一共享状态的激进模式
- 复杂评分引擎
- 长周期自治调度系统

## 3. 设计原则

1. 并发服务于交付，不为并发而并发。
2. 主控统一决策，子 agent 不各自扩散。
3. 先控制重复劳动，再追求更高并发数。
4. token 成本必须进入调度决策，而不是事后统计。
5. v1 先采用轻量规则型调度，不引入复杂学习系统。

## 4. 总体架构

本轮采用“主控调度 + 两类并发模式 + 汇总收敛”的结构。

### 4.1 主控调度器

主控 agent 负责：

- 接收用户目标
- 判断任务复杂度、依赖关系和并发收益
- 估算 token 成本等级
- 决定是否并发以及采用何种模式
- 在子 agent 完成后统一收口并形成最终交付

并发能力不下放给普通 agent，自始至终由主控统一管理。

### 4.2 任务切分器

任务切分只允许两种结构：

1. **独立子任务并行**
   - 适用于多个目标彼此独立
   - 例如并行研究多个模块、并行检查多份材料、并行回答多个互不依赖的问题

2. **同任务分阶段协同**
   - 适用于同一目标存在可分工阶段
   - 例如研究现状、生成方案、执行验证分别由不同 agent 负责

v1 不支持多个 agent 无边界地同时处理同一可变共享状态。

### 4.3 执行代理池

执行层使用可复用的角色池，例如：

- 研究 agent
- 规划 agent
- 实现 agent
- 验证 agent
- 审查 agent

主控按需分配，不固定常驻，也不默认并行启动全部角色。

### 4.4 结果汇总器

结果汇总器负责：

- 去重
- 冲突检测
- 缺口识别
- 统一摘要
- 将必要结论回收到主控上下文

并发产出不直接暴露给用户，用户看到的仍应是主控整合后的稳定结果。

## 5. 动态决策模型

### 5.1 三类调度结果

主控对每个任务只允许做出以下三类决策之一：

1. **独立子任务并行**
2. **同任务分阶段协同**
3. **禁止并发，退回串行**

### 5.2 独立子任务并行触发条件

满足以下多数条件时进入该模式：

- 子任务之间输入材料可以清晰切分
- 子任务几乎没有共享写状态
- 子任务之间不依赖彼此中间结果
- 汇总成本明显低于串行处理成本

### 5.3 同任务分阶段协同触发条件

满足以下多数条件时进入该模式：

- 整体目标单一，但内部存在明显阶段
- 阶段边界清楚
- 后一阶段可以消费前一阶段产物，而不是频繁来回交互
- 多角色分工能显著降低主窗口负担

### 5.4 禁止并发条件

出现以下情况之一时直接回退串行：

- 用户目标尚不清晰
- 多个 agent 会频繁竞争同一上下文
- 共享可变状态过多
- 任务规模不足以抵消并发开销
- 预计 token 成本高于可接受收益

## 6. Token 预算门控

### 6.1 预算等级

v1 采用轻量预算门控：

- **小任务**：默认不并发
- **中任务**：最多 2 个并发 agent
- **大任务**：最多 3-4 个并发 agent，但必须满足高独立性和低重叠风险

### 6.2 预算进入决策的方式

主控在发起并发前至少要判断：

- 子任务数量
- 每个子任务预期上下文长度
- 汇总所需额外成本
- 可能重复劳动的风险

若汇总成本和重复成本接近或超过并发收益，则不进入并发。

### 6.3 执行中途回收

主控必须支持在执行中途进行以下控制：

- 提前终止低价值 agent
- 对重叠产出进行去重
- 在发现依赖冲突后，将剩余流程降级为串行

这项机制是控制 token 成本的核心，不允许“放出去就全部跑完”。

## 7. 子 Agent 任务契约

每个子 agent 启动前都必须拿到统一契约，至少包含：

- 当前目标
- 任务边界
- 输入材料
- 预期产物
- 明确禁止重复的范围
- token 预算等级

这样可以降低以下问题：

- 多个 agent 做了同一件事
- 子 agent 自行扩展范围
- 主控难以汇总和比较结果

## 8. 收敛与完成判定

### 8.1 收敛职责

并发 agent 只负责加速产出，不负责最终完成判定。

最终完成必须由主控统一判断：

- 用户目标是否被完整覆盖
- 是否存在结论冲突
- 是否仍需最后一跳整合或验证
- 结果是否能直接交付给用户

### 8.2 冲突处理

当两个或多个 agent 结论不一致时：

1. 主控优先比较证据充分度
2. 需要时追加一个更小范围的核对任务
3. 不把未经消解的冲突直接暴露为最终结论

### 8.3 降级策略

若并发过程中发现：

- 重叠率过高
- 汇总成本过大
- 任务拆分不合理

则主控应立即回退为串行收敛，而不是继续扩散更多 agent。

## 9. 观测与验收

v1 只跟踪 4 个基础指标：

1. **总耗时**
2. **总 token 成本**
3. **重复工作率**
4. **一次性交付率**

这些指标足以支撑后续判断：

- 哪类任务适合并发
- 哪类任务不值得并发
- 预算上限是否需要调整

本轮不引入复杂评分模型。

## 10. 风险与约束

### 10.1 主要风险

1. 任务切分不准，导致重复劳动
2. 主控汇总成本过高，吞噬并发收益
3. token 预算约束过松，导致成本失控
4. 预算约束过严，导致并发能力形同虚设

### 10.2 控制方式

1. 只允许两种受控并发模式
2. 小任务默认不并发
3. 并发数量由预算和独立性共同决定
4. 设置中途回收与降级机制
5. 先用规则稳定行为，再考虑更复杂策略

## 11. v1 推荐落地顺序

1. 先定义任务画像字段和调度决策规则
2. 再实现两类并发模式的统一入口
3. 然后补齐子 agent 任务契约
4. 再实现结果汇总、冲突处理与降级
5. 最后补基础观测指标

## 12. 本轮结论

本轮推荐采用：

- **方案 B：主控 agent 动态决策 + token 预算约束的混合并发模型**

该方案在速度、稳定性和成本之间最平衡，适合作为 Copilot CLI 多 agent 协同能力的第一版正式设计。
