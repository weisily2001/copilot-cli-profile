# Copilot CLI 启动速度优化设计

## 1. 目标

在不破坏当前记忆生命周期与审批边界规则的前提下，优化 Copilot CLI 的启动体验，重点解决：

1. 冷启动体感偏慢
2. 启动链路中不必要的初始化与重复检查
3. 登录链路的重复探测与重复登录摩擦

本轮优化方向为：同时处理启动速度与登录摩擦，但优先聚焦冷启动耗时。

## 2. 现状结论

当前已确认的基础事实：

1. 本地登录态缓存存在，`config.json` 中已有 `lastLoggedInUser` 与 `loggedInUsers`
2. 当前项目记忆目录规模很小，不足以单独解释明显的启动慢问题
3. `session-start.ps1` 与 `session-end.ps1` 的单次执行耗时较低，说明记忆 hook 本身不是当前最主要的冷启动瓶颈

因此，本轮不假设“启动慢 = 记忆文件太多”，而是把问题收敛为：

- 启动链路可能存在多段初始化叠加
- 登录态检查可能存在不必要探测
- 指令、技能、环境和项目记忆恢复可能未做足够裁剪

## 3. 设计原则

1. 先观测，再优化，不凭感觉改启动链路
2. 只优化最慢的 1-2 段，不做大拆大改
3. 保持现有记忆生命周期与审批边界不回退
4. 启动优化优先改善体感，而不是引入复杂机制
5. 低风险信息读取与启动观测默认自动执行

## 4. 启动优化总体架构

本轮采用两段式方案：

### 4.1 启动观测层

先把启动链路拆段并记录耗时，至少覆盖：

- 全局指令加载
- 项目记忆恢复
- hook 执行
- 环境 / 插件 / 扩展信息加载
- 登录态检查

目标不是做长期监控，而是快速定位真实瓶颈。

### 4.2 最小化优化层

在观测结果基础上，只对最慢的 1-2 段做最小改动，例如：

- 延后加载
- 条件触发
- 短时缓存
- 跳过重复检查

不对整套启动流程做架构级重写。

## 5. 启动观测设计

### 5.1 观测产物

每次冷启动记录一份轻量结构化结果，至少包含：

- 总耗时
- 各阶段耗时
- 是否触发登录态检查
- 是否触发项目记忆恢复
- 当前项目键或项目根

### 5.2 存放位置

观测日志统一写入：

`C:\Users\HP\.copilot`

原因：

- 属于 Copilot CLI 全局运行信息
- 便于与全局配置和设计文档集中管理
- 不污染运行时项目记忆目录

### 5.3 保留策略

默认只保留最近少量记录，不做无限增长。

目标：

- 让日志足以辅助定位问题
- 不让观测本身反过来拖慢启动

## 6. 提速策略设计

### 6.1 记忆读取裁剪

启动时不做无差别全量恢复。

优化方向：

1. 仅在当前项目已启用项目记忆且相关文件存在时读取
2. 优先读取最轻量的 `handoff.md` / `state.md`
3. 仅在必要时继续读取 `decisions.md` / `overview.md`
4. 对不影响当前启动的长期知识避免过早读取

### 6.2 项目解析短时缓存

对“当前路径 → 项目键 / 项目根”的解析结果做短时缓存。

目标：

- 减少同一启动链路中的重复扫描
- 减少 hook 与恢复逻辑对相同路径的重复计算

### 6.3 非关键初始化延后

将不会影响当前会话立即可用性的初始化改为延后或条件触发，例如：

- 非关键环境信息整理
- 可由首次实际使用时再触发的扩展读取
- 非必须的补充型恢复逻辑

原则：

- 不影响正确性
- 不影响审批边界
- 不影响基础记忆恢复

### 6.4 登录链路稳态化

对登录状态增加“已缓存且有效则不重复探测”的规则。

目标：

- 减少不必要的登录检查
- 降低重复 `/login` 的触发概率

边界：

- 不绕过真正失效的登录态
- 不跳过必要的认证恢复
- 仅跳过明显重复且无价值的探测

## 7. 风险与约束

### 7.1 主要风险

1. 过度延后加载导致真正需要的信息没有及时可用
2. 缓存策略不当导致读取过期项目上下文
3. 登录探测裁剪过头导致失效状态未被及时发现

### 7.2 控制方式

1. 先观测，后裁剪
2. 缓存只做短时缓存，不做长期强缓存
3. 登录态只跳过重复探测，不跳过真实恢复
4. 每次优化后都要验证启动行为与当前规则一致

## 8. 验证思路

本轮实施后应验证：

1. 能拿到启动总耗时与分阶段耗时
2. 能确认最慢阶段，不再靠猜测优化
3. 冷启动路径中的不必要读取或探测被减少
4. 登录态稳定时不会频繁触发重复登录
5. 记忆恢复、审批边界、hook 行为不被破坏

## 9. 本轮推荐实现顺序

1. 先加轻量启动观测
2. 基于观测结果定位最慢阶段
3. 对最慢的 1-2 段做最小化优化
4. 再做登录链路的重复探测收敛

## 10. 本轮结论

本轮启动优化不做大规模重构，采用：

- **方案 3：先做启动观测，再做最小瘦身 + 登录稳态化**

这是当前最稳妥、最不容易误优化的路径。
