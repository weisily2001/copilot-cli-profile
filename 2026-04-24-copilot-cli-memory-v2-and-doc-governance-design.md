# Copilot CLI 记忆闭环 v2 与文档治理设计

## 1. 目标

让 Copilot CLI 朝以下方向持续增强：

- 更长记忆与跨会话连续性
- 更快理解用户需求
- 更贴合用户使用习惯
- 具备可控的自我学习与自我进化能力

本轮设计先解决两个基础问题：

1. 全局文档与项目文档的存放边界
2. 后续优化的优先级，先收敛到“记忆闭环 v2”

## 2. 文档与记忆的三层边界

### 2.1 全局配置与全局文档

路径：

`C:\Users\HP\.copilot`

职责：

- Copilot CLI 全局配置
- 全局工作内核
- 全局方法说明
- 全局演进设计文档
- 全局模板、技能、代理、hooks、MCP、LSP 配置

典型内容：

- `copilot-instructions.md`
- `settings.json`
- `mcp-config.json`
- `lsp-config.json`
- 全局设计文档与配置说明

### 2.2 项目正式文档

路径：

`E:\copilotcli\<项目名>`

职责：

- 具体项目的需求、设计、计划、验证、复盘
- 项目级说明文档
- 项目级配置说明

典型内容：

- 项目设计文档
- 项目实施计划
- 验证报告
- 项目交付说明

### 2.3 运行时项目记忆

路径：

`C:\Users\HP\.copilot\memory\projects\<project-id>`

职责：

- 仅作为 Copilot 恢复上下文的轻量记忆层
- 不承担项目正式文档仓库职责

典型内容：

- `handoff.md`
- `state.md`
- `decisions.md`
- `overview.md`

设计约束：

- 记忆层只保留短、小、可快速恢复上下文的内容
- 长文档与正式项目文档不写入记忆目录

## 3. 行为规则

### 3.1 全局规则

后续应将以下规则写入全局指令：

1. 涉及 Copilot CLI 全局能力、全局配置、全局流程的文档，写入 `C:\Users\HP\.copilot`
2. 涉及具体项目的设计、计划、验证、复盘文档，写入 `E:\copilotcli\<项目名>`
3. `C:\Users\HP\.copilot\memory\projects\<project-id>` 仅用于项目记忆恢复，不作为项目正式文档目录

### 3.2 模板规则

后续应将以下规则写入项目模板：

1. 新项目默认遵守项目文档目录规则
2. 项目模板只声明项目文档存放位置
3. 项目模板不自动创建项目记忆文件
4. 项目模板继续保留现有条件式记忆规则：存在才读取/更新，不存在仅提示启用位置

## 4. 记忆闭环 v2 的设计方向

### 4.1 目标

让 Copilot 不只是“能写记忆”，而是“知道该记什么、什么时候读、什么时候忽略”。

### 4.2 记忆分层

建议把记忆分成 4 层：

1. 用户长期偏好层
   - 语言偏好
   - 输出风格
   - 工作习惯
   - 常见禁止项

2. 项目长期知识层
   - 架构决策
   - 常用路径
   - 关键约束
   - 项目固定规则

3. 会话状态层
   - 当前阶段
   - 已完成项
   - 下一步
   - 阻塞点

4. 纠错学习层
   - 用户纠正过的误判
   - 应避免重复犯的理解错误
   - 对同类任务的更优处理方式

### 4.3 设计原则

1. 长期偏好与临时状态必须分开
2. 只有稳定结论才能进入长期层
3. 纠错记录需要可复用，不能只停留在聊天历史中
4. 读取时必须按任务类型裁剪，不做无差别全量加载

## 5. 优化优先级

### P0：记忆闭环 v2

先建立稳定的记忆写入、读取和压缩规则。

### P1：纠错学习层

把“不是这个意思”“以后别这样做”这一类反馈沉淀成稳定偏好。

### P2：需求理解加速层

在回答前先识别任务类型，例如：

- 问答
- 规划
- 执行
- 调试
- 收尾

### P3：自进化层

基于任务结果和纠错记录做轻量复盘，逐步优化策略。

## 6. 推荐方案

当前推荐采用以下组合：

1. 文档治理采用“双层硬规则”
2. 全局规则固化到 `C:\Users\HP\.copilot` 下的全局指令与相关文档
3. 项目模板同步固化项目文档位置约定
4. 下一轮只做“记忆闭环 v2”设计与实现，不同时展开完整自进化体系

## 7. 本轮结论

本轮不直接进入实现，先把方向固定为：

- 全局/重要配置文档 → `C:\Users\HP\.copilot`
- 项目正式文档 → `E:\copilotcli\<项目名>`
- 运行时项目记忆 → `C:\Users\HP\.copilot\memory\projects\<project-id>`
- 后续优化优先级 → P0 记忆闭环 v2 → P1 纠错学习 → P2 需求理解加速 → P3 自进化
