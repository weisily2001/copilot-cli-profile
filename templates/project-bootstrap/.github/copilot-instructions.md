# 仓库级 Copilot 指令

1. 复杂任务优先进入计划模式。
2. 实现前至少阅读相关文件与已有模式。
3. 若项目已启用项目记忆目录且存在对应文件，再在结束前更新 `handoff.md` 与 `state.md`；若尚未启用，仅提示可选记忆位置，不主动创建文件。
4. 外部依赖优先查官方文档。
5. 当前项目的正式文档默认写入 `E:\copilotcli` 下与当前项目同名的目录，不要把长文档写入项目记忆目录。
6. 低风险命令默认自动执行；高风险或风险不明动作先获批。
7. 项目记忆目录中的短期任务记忆应在收尾时压缩并清理。
