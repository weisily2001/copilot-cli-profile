# Copilot CLI 全局记忆组织与技能沉淀 v1 设计

## 1. 目标

在不改动项目层记忆目录、不引入重型自治系统的前提下，基于现有 `C:\Users\HP\.copilot` 体系完成一次增量升级：

1. 让全局记忆具备清晰分层。
2. 让任务结束后的有效经验可以自动沉淀。
3. 让已有 skills、偏好、纠错记录不再分散存在，而是可以被统一索引和路由。
4. 让长期层只保留稳定、已验证、跨任务可复用的信息。

本轮只覆盖全局层：`C:\Users\HP\.copilot`。

## 2. 现状结论

当前全局层已经具备较强基础：

1. `copilot-instructions.md` 已定义全局工作内核、记忆规则、审批边界和并行策略。
2. `memory-governance.md` 与 `memory-lifecycle.md` 已定义文档边界、短长期分层和压缩升级规则。
3. `memory\global\profile.md`、`preferences.json`、`corrections.md` 已分别承载偏好摘要、结构化偏好和纠错学习。
4. `.copilot\skills` 已有一批全局 skills，但缺少统一登记与索引。
5. `hooks\ecc-memory\session-start.ps1` 与 `session-end.ps1` 已经是自动沉淀的天然挂点。

因此，本轮不重建体系，只补三类缺口：

1. 缺少轻量索引层。
2. 缺少自动沉淀的判定与路由规则。
3. 缺少对 skills 与 SOP 的统一登记层。

## 3. 设计原则

1. 增量治理，不推翻现有文件职责。
2. 自动沉淀默认开启，但升级门槛严格。
3. 宁可漏记，也不滥记。
4. 短期状态与长期知识严格分开。
5. 写入前必须去重和分类，不能把不确定内容猜着写进长期层。
6. hooks 失败只影响本次沉淀，不影响正常会话与既有配置。

## 4. 分层结构

### 4.1 L0 行为内核层

继续由以下文件承担：

- `C:\Users\HP\.copilot\copilot-instructions.md`
- `C:\Users\HP\.copilot\memory-governance.md`
- `C:\Users\HP\.copilot\memory-lifecycle.md`

职责：

- 定义行为边界。
- 定义何时读取、何时写入、何时升级。
- 定义高风险审批边界与自动执行边界。

### 4.2 L1 记忆索引层

新增：

- `C:\Users\HP\.copilot\memory\global\memory-index.md`

职责：

- 只记录“场景/触发词 -> 去哪里读 -> 用哪个 skill”。
- 不写细节，不写过程，不写临时状态。
- 用最短路径帮助后续任务先定位，再按需读细节。

设计要求：

1. 保持轻量，面向命中率而不是信息量。
2. 只记录高频场景、关键全局规则和高复用 skill 入口。
3. 一条索引只指向一个最优入口，避免多头路由。

### 4.3 L2 稳定全局事实层

保留并明确现有文件职责：

- `profile.md`：口语化偏好摘要。
- `preferences.json`：结构化偏好与机器可读配置。
- `corrections.md`：稳定纠错学习。

新增：

- `C:\Users\HP\.copilot\memory\global\global-facts.md`

职责：

- 承载不适合进入上述三类文件、但又长期有效的全局事实。
- 只保留跨任务仍稳定成立的约束、常量、工作方法边界。

禁止写入：

- 临时任务状态。
- 一次性调试记录。
- 具体会话结论。
- 未经验证的假设。

### 4.4 L3 技能与 SOP 沉淀层

保留：

- `C:\Users\HP\.copilot\skills\`

新增：

- `C:\Users\HP\.copilot\skills\skill-registry.json`

职责：

- 为现有 skills 建立统一登记层。
- 补齐用途、触发词、适用范围、稳定度、关联文档等元数据。
- 允许 skill 与非 skill SOP 被统一挂接到索引层。

设计效果：

1. 技能不再只是“目录存在”，而是“可发现、可命中、可路由”。
2. 后续新增 skill 时可以同步登记，不需要靠记忆猜测。

### 4.5 L4 会话归档层

继续复用：

- `C:\Users\HP\.copilot\session-state\`
- 现有 memory hooks 产出的短期摘要

职责：

- 作为短期归档与提炼来源。
- 不直接承担长期知识存储职责。

关键边界：

1. 所有新内容默认先进入 L4。
2. 只有通过规则判定后，才允许升级到 L1/L2/L3。
3. 没有升级价值的内容只压缩，不长期保存。

## 5. 自动沉淀流程

本轮采用“窄流水线、严门槛”的自动沉淀方案。

### 5.1 触发点

仅在以下场景运行：

1. 会话结束。
2. 阶段性收尾。
3. 用户明确表达“记住”“以后都这样”。
4. hooks 已经捕捉到可交接摘要时。

### 5.2 五步流水线

#### 第一步：候选提取

从以下来源提取候选条目：

- 本轮总结
- `session-state`
- handoff/state 类摘要
- hooks 捕获的短期信息

说明：

- 这一步只生成候选，不直接写长期层。

#### 第二步：分类判定

只允许分到以下 4 类：

1. 稳定偏好
2. 明确纠错
3. 全局 SOP / skill 线索
4. 短期状态摘要

如果无法明确分类，直接留在 L4，不升级。

#### 第三步：写入门槛

只有同时满足以下 4 条才允许升级：

1. 已验证
2. 可复用
3. 跨任务仍有价值
4. 不是临时状态

任一条件不满足，都不升级。

#### 第四步：去重合并

写入前必须先比对：

- `profile.md`
- `preferences.json`
- `corrections.md`
- `skill-registry.json`
- `memory-index.md`

处理规则：

1. 同义内容优先合并。
2. 冲突内容优先更新已有条目，不重复创建。
3. 无法判定的冲突回退到 L4，等待后续人工确认或再次验证。

#### 第五步：索引刷新

一旦写入 L2/L3，同步刷新 L1：

- 新增或更新触发词。
- 标注读取入口。
- 标记优先 skill 或规则文件。

目标是让后续任务先命中索引，再按索引读取细节。

## 6. 新增文件设计

### 6.1 `memory\global\memory-index.md`

内容建议：

1. 高频场景索引
2. 关键纠错入口
3. 高复用 skill 入口
4. 少量全局红线规则

每条记录格式建议保持统一：

`场景/触发词 -> 文件或 skill -> 备注（可选）`

### 6.2 `memory\global\global-facts.md`

内容建议：

1. 全局稳定事实
2. 长期有效的运行边界
3. 不适合写入偏好/纠错文件的全局约束

### 6.3 `skills\skill-registry.json`

每个 skill 至少记录：

- `name`
- `purpose`
- `triggers`
- `scope`
- `stability`
- `relatedDocs`
- `relatedMemory`

### 6.4 `memory\global\distillation-rules.json`

职责：

- 定义自动沉淀的路由规则、写入条件、禁止升级条件和回退策略。

至少包含：

1. 可升级类型
2. 禁止升级信号
3. 目标写入位置
4. 去重策略
5. 索引刷新策略

## 7. 迁移策略

采用保守迁移。

### 7.1 不做的事

1. 不重写现有 `profile.md`、`preferences.json`、`corrections.md` 的语义。
2. 不批量整理项目层记忆。
3. 不把 session-state 直接升格为长期记忆。
4. 不引入大规模自治技能生成。

### 7.2 要做的事

1. 先生成索引与规则层。
2. 再把现有 global skills 纳入 registry。
3. 再建立 hooks 到规则层的稳定映射。
4. 最后再让自动沉淀接管新增内容的升级。

## 8. 错误处理

1. hooks 失败时只记录失败，不中断正常会话。
2. 自动沉淀失败时，不覆盖已有长期文件。
3. 去重和分类失败时，直接回退到 L4。
4. 任一条目不满足升级门槛时，只压缩，不长期存储。

## 9. 验收标准

完成后应满足：

1. 所有全局记忆都能明确分到 L1/L2/L3/L4。
2. 至少一个已有 skill 能通过 `skill-registry.json` 与 `memory-index.md` 被稳定命中。
3. 任务结束后，稳定偏好、明确纠错、skill 线索能自动路由到正确位置。
4. 临时调试信息不会进入长期层。
5. 现有全局文件职责不冲突，启动和会话流程不被破坏。

## 10. 推荐实施顺序

1. 新增 `memory-index.md`
2. 新增 `global-facts.md`
3. 新增 `skill-registry.json`
4. 新增 `distillation-rules.json`
5. 调整 hooks，使其接入规则层
6. 更新全局文档，补充新的分层与沉淀说明

## 11. 结论

本轮推荐采用：

**方案 B：治理结构 + 自动沉淀流程**

它最适合你当前的 Copilot CLI 全局层现状：

1. 不推翻已有体系。
2. 能借鉴 GenericAgent 的“分层记忆 + 索引 + 技能沉淀”思想。
3. 自动沉淀默认开启，但通过严门槛避免噪声污染长期层。
4. 为后续是否继续做更强的 skill 自动生成保留扩展空间。
