# Copilot CLI 记忆生命周期与审批边界设计

## 1. 目标

在现有“记忆闭环 v2”基础上，补齐两项关键能力：

1. 让新增记忆自动区分长期与短期，短期记忆在任务结束后自动压缩，并清理无保留价值内容，降低存储膨胀与恢复成本。
2. 让 Copilot CLI 对执行审批边界有稳定、一致、可复用的规则，除高风险动作外默认自动执行，减少低风险操作上的确认摩擦。

## 2. 范围

本轮只覆盖记忆系统与审批边界：

- 长期/短期记忆分层规则
- 短期记忆压缩规则
- 短期记忆自动清理规则
- 审批矩阵与灰区处理规则
- 全局指令、偏好层、纠错层、模板与项目记忆技能中的规则同步

本轮不覆盖：

- 启动速度优化
- 登录链路优化
- 并行执行框架改造
- 复杂评分引擎或自适应学习系统

## 3. 设计原则

1. 规则优先于复杂智能：先用清晰规则稳定行为，再考虑更复杂策略。
2. 长期与短期分离：避免把过程性上下文直接沉淀为长期记忆。
3. 自动化默认开启：低风险动作应自动执行，减少不必要确认。
4. 高风险边界明确：系统安全、全局配置重大变更、隐私和重要数据操作必须先获批。
5. 轻量恢复优先：会话恢复应优先读取短、小、可快速理解的摘要，而不是累积长文档。

## 4. 记忆分层模型

记忆体系统一为 4 层：

### 4.1 用户长期偏好层

存放稳定偏好，例如：

- 输出风格
- 工作习惯
- 确认边界
- 长期优化方向

推荐落点：

- `C:\Users\HP\.copilot\memory\global\profile.md`
- `C:\Users\HP\.copilot\memory\global\preferences.json`

### 4.2 项目长期知识层

存放可跨会话复用的项目级稳定知识，例如：

- 关键路径
- 架构决策
- 固定约束
- 项目长期规则

推荐落点：

- `C:\Users\HP\.copilot\memory\projects\<project-id>\decisions.md`
- `C:\Users\HP\.copilot\memory\projects\<project-id>\overview.md`

### 4.3 短期任务状态层

存放当前任务进行中所需的临时上下文，例如：

- 当前阶段
- 已完成项
- 下一步
- 阻塞点
- 临时引用路径

推荐落点：

- `C:\Users\HP\.copilot\memory\projects\<project-id>\handoff.md`
- `C:\Users\HP\.copilot\memory\projects\<project-id>\state.md`

### 4.4 纠错学习层

存放用户明确纠正过的稳定规则，例如：

- 对表达和流程的纠正
- 同类误判的避免事项
- 对审批和记忆边界的明确要求

推荐落点：

- `C:\Users\HP\.copilot\memory\global\corrections.md`

## 5. 新增记忆的分流规则

新增记忆默认先进入短期任务状态层。

只有满足以下任一条件，才允许升级写入长期层：

1. 属于稳定用户偏好
2. 属于明确纠错规则
3. 属于可跨会话复用的项目长期知识

以下内容不得直接进入长期层：

- 临时执行日志
- 过程性命令输出
- 一次性排查路径
- 已失效的中间推理
- 可由正式文档替代的长文本

## 6. 短期记忆压缩与清理

### 6.1 压缩触发时机

以下时机触发短期记忆压缩：

1. 任务完成前
2. 窗口切换前
3. 用户明确要求“记住今天状态，明天继续”
4. 短期记忆内容明显膨胀时

### 6.2 压缩产物

压缩后的短期记忆应收敛为单屏可读摘要，至少包含：

- 当前目标
- 当前阶段
- 已完成项
- 下一步
- 阻塞点
- 必要引用路径

### 6.3 自动清理规则

压缩完成后，自动清理无复用价值的短期内容，只保留：

- 当前状态摘要
- 下一步
- 阻塞点
- 必要引用路径

以下内容应默认清理：

- 冗长过程描述
- 已完成且无复用价值的中间步骤
- 重复信息
- 临时调试记录
- 可从正式文档或长期层恢复的信息

### 6.4 清理后的升级判断

清理前应先判断是否存在需要升级保留的稳定结论：

- 如果是稳定偏好，写入全局偏好层
- 如果是明确纠错，写入纠错学习层
- 如果是项目长期知识，写入项目长期知识层
- 如果都不是，则只保留压缩后的短期摘要

## 7. 审批边界设计

### 7.1 默认自动执行

以下动作默认自动执行：

- 读取记忆文件
- 搜索文件和内容
- 常规检查
- 低风险命令
- 短期记忆压缩
- 短期记忆清理
- 低风险配置读取

### 7.2 必须先获批

以下动作必须先获批：

- 系统安全相关操作
- 全局配置重大变更
- 重构
- 删除文件
- 操作本机重要设置
- 涉及个人隐私的数据处理
- 重要数据上传
- 重要数据删除

### 7.3 灰区处理

若动作是否高风险存在歧义，按高风险处理，先告知影响并请求批准。

## 8. 文件与规则同步面

本轮设计预计同步到以下位置：

- `C:\Users\HP\.copilot\copilot-instructions.md`
- `C:\Users\HP\.copilot\memory\global\profile.md`
- `C:\Users\HP\.copilot\memory\global\preferences.json`
- `C:\Users\HP\.copilot\memory\global\corrections.md`
- `C:\Users\HP\.copilot\skills\memory-handoff\SKILL.md`
- `C:\Users\HP\.copilot\skills\preference-learning\SKILL.md`
- `C:\Users\HP\.copilot\templates\project-bootstrap\.github\copilot-instructions.md`
- `C:\Users\HP\.copilot\templates\project-bootstrap\.github\instructions\memory-hydration.instructions.md`

如需要新增专门规则文件，优先放在 `C:\Users\HP\.copilot` 或模板指令目录，不写入运行时项目记忆目录。

## 9. 错误处理与回退

1. 如果无法判断一条记忆是否值得长期保留，默认留在短期层，不直接升级。
2. 如果无法判断一项动作是否属于高风险，默认进入审批。
3. 如果短期记忆文件不存在，只提示可选位置，不自动创建。
4. 如果目标目录不是 Git 仓库，本轮只落盘文档与规则，不做提交要求。

## 10. 验证思路

本轮实施后应验证：

1. 新增规则能明确区分长期与短期记忆
2. 短期记忆在任务收尾时会被压缩成单屏摘要
3. 无保留价值内容会自动清理
4. 高风险动作需要批准，低风险动作默认自动执行
5. 模板与全局规则保持一致

## 11. 本轮推荐实现策略

采用轻量规则型方案：

1. 先固化规则，不引入复杂评分引擎
2. 先补齐全局指令、偏好层、纠错层、技能和模板
3. 后续如规则稳定，再考虑增加更细的保留时长、标签或轻量评分
